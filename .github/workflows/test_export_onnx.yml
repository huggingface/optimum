name: Exporters ONNX / Python - Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        python-version: [3.8, 3.9]

    runs-on: [intel-cpu, 8-cpu, ci]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies for pytorch export
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir torch==2.1.2 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install .[tests,exporters]

      - name: Test with pytest
        working-directory: tests
        run: |
          pytest exporters/onnx -m "not tensorflow_test and not timm_test" -s -vvvv --durations=0

      - name: Install dependencies for tensorflow export
        run: |
          pip install --no-cache-dir torch==2.1.2 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install .[tests,exporters-tf]

      - name: Test with pytest
        working-directory: tests
        run: |
          pytest exporters/onnx -m "tensorflow_test" -s -vvvv --durations=0
